## Load balance using nginx and docker-compose

this code is a simple example of how to build a load-balance system using NGINX + Apache + Docker-compose 
<hr>

## required commands:

**docker-compose up -d**